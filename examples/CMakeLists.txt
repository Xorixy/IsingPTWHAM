if(ISING_BUILD_EXAMPLES)
    cmake_minimum_required(VERSION 3.24)
    message(STATUS "Build examples ON")
    add_subdirectory(10A-mpi-helloworld)
    add_subdirectory(10B-mpi-bcast)
    add_subdirectory(10C-mpi-wrap)
endif()
